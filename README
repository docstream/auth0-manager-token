NodeJS func caching and requesting TOKEN for Auth0 Management API. 

exports =

    (clientID , clientSecret, domain, cb) ->


# ---------

amt = require 'auth0-manager-token'
amt 'myId..', 'long-secret..', 'my-name.eu.auth0.com', (err,token) ->
	authzHeader = token
	...

# -----------